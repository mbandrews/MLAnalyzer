
#include "MLAnalyzer/RecHitAnalyzer/interface/RecHitAnalyzer.h"



// Initialize branches ____________________________________________________________//

// Fill TRK rechits at EB/EE ______________________________________________________________//
void RecHitAnalyzer::TrackMatching ( const edm::Event& iEvent, const edm::EventSetup& iSetup ) {

  
  // std::cout<< "Reached Track matcher" << std::endl;
  // edm::Handle<reco::TrackCollection> tracks;
  // iEvent.getByToken( trackCollectionT_, tracks );

  // edm::Handle<PFCollection> PFCandidates;
  // iEvent.getByToken( pfCollectionT_, PFCandidates );

  // matchTrackPF(tracks, PFCandidates);



    } 
